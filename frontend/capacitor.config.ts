import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lymbia.gastroassistant',
  appName: 'GastroAssistant',
  webDir: 'web-build'
};

export default config;
